services:
  flix-finder:
    build: .
    container_name: flix-finder
    restart: unless-stopped
    ports:
      - "7000:7000"
    environment:
      - PORT=7000
      # Add optional subpath if running behind a reverse proxy (e.g. /flix-finder)
      # - APP_BASE_PATH=/flix-finder
